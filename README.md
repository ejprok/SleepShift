# TemplateApp

A basic iOS template app built with Swift and UIKit. No storyboard - everything is code-based.

## Setup

Run `rename.sh` to rename the project: `./rename.sh "Your App Name" com.yourorg`
